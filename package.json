{
  "name": "pegs-js",
  "version": "0.0.0",
  "private": true,
  "description": "The game Pegs for the TI-83+ graphing calculator, ported to the browser.",
  "scripts": {
    "start": "parcel index.html",
    "build-production": "parcel build index.html --public-url './'",
    "test": "mocha --require coffeescript/register js/test/*",
    "test-watch": "mocha --require coffeescript/register --watch --watch-extensions coffee --reporter min js/test/*"
  },
  "author": "Rory O’Kane",
  "devDependencies": {
    "chai": "^4.2.0",
    "coffeescript": "^2.4.1",
    "mocha": "^6.2.0",
    "parcel-bundler": "^1.12.3",
    "sass": "^1.23.0-module.beta.1"
  },
  "dependencies": {
    "jquery": "^3.4.1",
    "keymage": "^1.1.3",
    "lodash": "^4.7.0"
  }
}
